---
"@paras.in/pui": minor
---

Added a `readOnly` mode to the `FileExplorer` component which hides the creation toolbar and context menus, makes `onItemsChange` optional, and preserves consistent layout spacing. Centralized public API type exports and optimized the CI workflow triggers.
